import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Akemis - Global Consulting Firm",
  description: "Accelerate Your Growth with Global Consulting Expertise. We help businesses in Hong Kong and beyond optimize operations, integrate solutions, and scale efficiently.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
